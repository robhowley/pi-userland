# pi-structured-return

A [pi](https://pi.dev) extension that provides structured return handling and parser support for better command output parsing.

## Installation

```shell
pi install npm:@robhowley/pi-structured-return
```
