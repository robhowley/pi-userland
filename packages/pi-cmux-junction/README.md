# pi-cmux-junction

Fork and run parallel Pi sessions in isolated Git worktrees and cmux workspaces, with fast session jumping and safe teardown.

## Installation

```shell
pi install npm:@robhowley/pi-cmux-junction
```
